- hosts: localhost
  become: true
  pre_tasks:
    - name: Update package repos
      apt:
        update_cache: yes
      changed_when: False

  tasks:
    - include: tasks/packages.yml
    - include: tasks/docker.yml
      tags: [docker]
